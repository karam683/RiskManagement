module.exports = Object.assign (
    cds_bind, {help: `
# SYNOPSIS

    *cds bind* <service> [<options>]

    Binds the given service to

# OPTIONS

    *-2|--to* <credentials>

`})

function cds_bind (projects, options) { // eslint-disable-line
  console.log (`cds bind is not yet released`)
}
